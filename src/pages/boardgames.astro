---
import PageLayout from "@/layouts/Base.astro";

const meta = {
	title: "Juegos de Mesa",
	description: "Mi colección de juegos de mesa y partidas en BoardGameGeek",
};
---

<PageLayout meta={meta}>
	<div class="space-y-6">
		<h1 class="title">Juegos de Mesa</h1>
		<p>Aquí está mi colección de juegos de mesa y actividad de juego rastreada en BoardGameGeek.</p>

		<div class="alert">
			<p>
				Debido a restricciones de seguridad, el visor de la colección no se puede incrustar directamente.
				Por favor haz clic en el botón de abajo para ver mi colección de BoardGameGeek en una nueva ventana:
			</p>
			<a
				href="https://playback.geekgroup.app/cardila"
				target="_blank"
				rel="noopener noreferrer"
				class="view-button"
			>
				Ver Mi Colección de Juegos de Mesa
			</a>
		</div>

		<div class="collection-info">
			<h2>Lo que encontrarás:</h2>
			<ul>
				<li>Mi colección completa de juegos de mesa</li>
				<li>Partidas recientes y actividad de juego</li>
				<li>Calificaciones y reseñas</li>
				<li>Estadísticas de juegos</li>
			</ul>
		</div>
	</div>
</PageLayout>

<style>
	.alert {
		background-color: var(--theme-bg-offset);
		border: 1px solid var(--theme-divider);
		border-radius: 0.5rem;
		padding: 1.5rem;
		margin-top: 2rem;
		text-align: center;
	}

	.view-button {
		display: inline-block;
		margin-top: 1rem;
		padding: 0.75rem 2rem;
		background-color: var(--color-accent);
		color: var(--color-accent-2);
		text-decoration: none;
		border-radius: 0.5rem;
		font-weight: 600;
		transition: all 0.2s;
	}

	.view-button:hover {
		opacity: 0.9;
		transform: translateY(-2px);
	}

	.collection-info {
		margin-top: 2rem;
		padding: 1.5rem;
		background-color: var(--theme-bg-offset);
		border-radius: 0.5rem;
	}

	.collection-info h2 {
		margin-bottom: 1rem;
		font-size: 1.25rem;
		font-weight: 600;
	}

	.collection-info ul {
		list-style: disc;
		margin-left: 1.5rem;
	}

	.collection-info li {
		margin-bottom: 0.5rem;
	}
</style>
