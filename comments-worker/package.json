{
  "name": "blog-comments-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 execute blog-comments --local --file=./migrations/001_initial.sql",
    "migrate:remote": "wrangler d1 execute blog-comments --remote --file=./migrations/001_initial.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260829.1",
    "typescript": "^7.0.2",
    "wrangler": "^4.127.1"
  }
}
